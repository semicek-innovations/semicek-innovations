<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <title>Reset Your Password</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f9f9fb;
      margin: 0;
      padding: 0;
      color: #333;
    }

    .container {
      max-width: 600px;
      margin: 40px auto;
      background-color: #ffffff;
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 4px 16px rgba(0, 0, 0, 0.06);
    }

    .header {
      background-color: #4c1d95;
      padding: 20px;
      text-align: center;
    }

    .header img {
      max-height: 50px;
    }

    .content {
      padding: 32px;
    }

    .content h1 {
      font-size: 22px;
      color: #4c1d95;
      margin-bottom: 18px;
    }

    .content p {
      font-size: 16px;
      line-height: 1.6;
    }

    .button {
      display: inline-block;
      background-color: #7c3aed;
      color: #ffffff;
      text-decoration: none;
      padding: 14px 24px;
      margin: 30px 0;
      border-radius: 8px;
      font-weight: 600;
      font-size: 15px;
    }

    .footer {
      background-color: #f3f4f6;
      padding: 24px;
      text-align: center;
      font-size: 13px;
      color: #6b7280;
    }

    .footer a {
      color: #7c3aed;
      text-decoration: none;
    }
  </style>
</head>

<body>
  <div class="container">
    <div class="header">
      <img src="cid:semicek-logo" alt="Semicek Innovations" />
    </div>
    <div class="content">
      <h1>{{title}}</h1>
      <p>{{greeting}}</p>
      <p>{{instructions}}</p>
      <p style="text-align: center;">
        <a href="{{resetLink}}" class="button">{{button}}</a>
      </p>
      <p>{{ignore}}</p>
      <p>{{{closing}}}</p>
    </div>
    <div class="footer">
      {{{copyright}}}<br />
      <a href="mailto:support@semicekinnovations.com">{{support}}</a>
    </div>
  </div>
</body>

</html>